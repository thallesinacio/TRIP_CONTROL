# Parametros de conexao do banco relacional local (RNF05).
# O modo JDBC usa esta configuracao para conectar ao PostgreSQL local.
# Cada chave pode ser sobrescrita por variavel de ambiente, ex.: TRIPCONTROL_DB_HOST.

db.host=localhost
db.port=5432
db.name=tripcontrol
db.user=postgres
db.password=24nu4agQ#
