# Parametros de conexao do banco relacional local (RNF05).
# ATENCAO: nenhum arquivo do projeto abre conexao nesta fase -- os dados ficam
# nos repositorios em memoria. Este arquivo existe para a virada ao PostgreSQL.
# Cada chave pode ser sobrescrita por variavel de ambiente, ex.: TRIPCONTROL_DB_HOST.

db.host=localhost
db.port=5432
db.name=tripcontrol
db.user=postgres
db.password=
